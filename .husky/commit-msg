commit_msg_file="$1"
commit_msg=$(cat "$commit_msg_file")

if ! echo "$commit_msg" | grep -qE "EDT-[0-9]+"; then
  echo ""
  echo "🚫 COMMIT REFUSÉ"
  echo "👉 Le message doit contenir une clé Jira"
  echo "✅ Exemple : Feature/EDT-39 initialise frontend project"
  echo ""
  exit 1
fi
